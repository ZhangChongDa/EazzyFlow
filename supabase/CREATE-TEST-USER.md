# 👤 创建测试用户指南

## 📋 方法 1: 在 Supabase Dashboard 中创建（推荐）

### 步骤：

1. **打开 Supabase Dashboard**
   - 访问 https://app.supabase.com/
   - 选择您的项目

2. **进入 Authentication**
   - 点击左侧菜单的 **Authentication**
   - 选择 **Users** 标签

3. **创建新用户**
   - 点击 **Add user** 或 **Create new user**
   - 选择 **Create user**（不是邀请）
   - 输入：
     - **Email**: `admin@eazzyflow.com`（或任何邮箱）
     - **Password**: `admin123456`（至少 6 个字符）
   - 点击 **Create user**

4. **完成！**
   - 用户已创建，可以直接使用这个邮箱和密码登录

## 📋 方法 2: 使用 SQL 创建（高级）

在 Supabase Dashboard → SQL Editor 中执行：

```sql
-- 创建用户（需要先创建 auth.users 记录）
-- 注意：这需要管理员权限，通常通过 Dashboard 创建更简单
```

## 📋 方法 3: 使用应用注册功能

1. **启动应用**
   ```bash
   npm run dev
   ```

2. **打开登录页面**
   - 应用会自动显示登录界面

3. **点击"没有账户？注册"**
   - 输入邮箱和密码
   - 点击注册

4. **注意邮箱确认**
   - 如果 Supabase 启用了邮箱确认，需要：
     - 检查邮箱收件箱
     - 点击确认链接
   - 或者禁用邮箱确认（见下方）

## 🔧 禁用邮箱确认（开发环境）

如果不想每次注册都确认邮箱，可以在 Supabase Dashboard 中：

1. **进入 Authentication → Settings**
2. **找到 "Email Auth" 部分**
3. **取消勾选 "Enable email confirmations"**
4. **保存**

这样注册后可以直接登录，无需邮箱确认。

## ✅ 测试用户凭据示例

创建以下测试用户：

- **管理员账户**:
  - Email: `admin@eazzyflow.com`
  - Password: `admin123456`

- **测试账户**:
  - Email: `test@eazzyflow.com`
  - Password: `test123456`

## 🔐 安全提示

- 生产环境应该启用邮箱确认
- 使用强密码策略
- 定期更换密码
- 不要在生产环境使用简单密码


